name: Deploy to Hostinger

on:
  push:
    branches: [main]

# Ce workflow est désactivé — le déploiement se fait via webhook direct
# GitHub → korilab.dev/deploy.php (sans GitHub Actions, sans billing)
